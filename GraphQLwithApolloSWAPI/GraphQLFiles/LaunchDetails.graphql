
query LaunchDetails($launchId: ID!) {
  launch(id: $launchId) {
    isBooked
    site
    id
    rocket {
      name
    }
    mission {
      missionPatch
      name
    }
  }
  me {
    email
  }
}
